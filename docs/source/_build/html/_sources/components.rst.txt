Components
======================================

Components are used to represent the properties of an entity. A single entity can be linked to :math:`0..n` components. But an entity cannot be linked to multiple components of the same type. A component is always linked to exactly one entity.

.. toctree::
   :maxdepth: 2
   :caption: List of components
   :glob:

   components/*

