namespace MiniEngine.Gui
{
    public static class ToolSelector
    {
        private static Tool? wipTool = null;

        public bool SelectFloat(Tool currentTool, out Tool newTool)
        {
            if (wipTool != null)
            {
                // TODO: take code in EditorState.cs and make a proper pop-up
            }
        }
    }
}
